# Stage 13 — Exact Local Causal Materialization

## Frozen base

- Repository: `C:\Users\D-Day\ProvenanceEsoterica`
- Branch: `provenance/client-spike`
- Base commit: `8dfddc1e` (`L1 grass: certify representative textured card load`)

## Implemented boundary

Stage 13 materializes one complete 8 m terrain package around the certified
Stage-12 quartz breach. It records 4 m of local depth in integer 12.5 cm
occupancy/material columns. The surrounding 192 m live terrain remains the
unchanged Stage-12 package stream through a deterministic 8 m handoff collar.

Materialization changes representation only. Every stored material run retains
the Stage-12 material, `FeatureId`, formation, structural normal, chronology,
event/fault ancestry, deposit-system ID, and deposit-body ID. Grade is not an
input to Stage-13 geometry.

## Player entry

- In-game menu: `INTEGRATION` → `Stage 13 - Exact Local Materialization`
- Direct playable launch: `--play-stage13-materialization`
- The stage opens at the certified quartz outcrop. X-ray remains presentation
  only and can inspect the same body below the surface.

## Permanent certificates

- `--cert-stage13-materialization`
  - 4,225 exact columns; 7,402 causal material runs
  - 401 quartz runs; 34 surface-quartz columns
  - monolithic/tiled occupancy and geometry parity
  - Stage-12 disable/control parity
  - render/collision boundary parity
  - buried x-ray quartz identity parity
- `--cert-stage13-materialization-visual`
  - player-scale runtime x-ray receipt
  - surface and terminal quartz `FeatureId da10b0d1e5f01001`
- `--cert-worldgen-cardinal-replacement-stage13`
  - complete 192 m residency through 408 m outbound/return sweeps
  - walk, sprint, and free-flight in N/E/S/W
  - exact package, geometry, material/feature, collision, image, and return parity
- `--cert-playable-runtime-independence`
  - cold → 13, 12 → 13, 13 → 12, and 13 → 13 order independence

## Performance result

The first Stage-13 sweep exposed a duplicate synchronous 64 m one-metre cell
cache. That cache was not terrain authority: the complete 192 m render/collision
answer already came from the bounded worker package stream. Stage 13 now retains
only a 16 m local diagnostic/interaction cache with a 20 m eviction bound.

- resident terrain packages: 2,601 (unchanged)
- maximum diagnostic cells: 975 (previously about 13,600)
- repeated cardinal movement worst: 12.204–13.251 ms
- movement frames over 16.667 ms: 0
- L1 grass sentinel: PASS, worst 13.705 ms on attribution rerun
- L6 combined sentinel: PASS, worst 13.384 ms

One intervening L1 execution recorded GPU-completion stalls up to 85.682 ms
with terrain at 0 ms and grass near 3 ms. It did not reproduce in the isolated
L1 rerun, and both adjacent L6 runs passed. Keep this as presentation-backend
telemetry, not as a Stage-13 terrain attribution.

## Closed systems

No mutation, pick fracture, detached bodies, water, active erosion, sediment
transport, fault changes, or P5b were opened. Stage 14 remains the next cut and
must use this exact body rather than minting a separate mineable representation.
